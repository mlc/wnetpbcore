<%- title "Edit Picklists" -%>
<p>Select a type of picklist to edit:</p>
<ul>
  <%- @controllers.each do |controller| %>
    <li><%= link_to controller.gsub('_', ' ').titlecase, :controller => controller, :action => 'index' %></li>
  <%- end -%>
</ul>