<%-
unless value.nil? || (value.respond_to?(:empty?) && value.empty?)
  if local_assigns[:field]
    val = value.send(field)
  else
    val = value
  end
-%>
  <dt><%= h key %></dt>
  <dd><%= ((local_assigns[:url] && val =~ /^http/) ? link_to(h(val),h(val)) : h(val)) %></dd>
<%-
end
-%>